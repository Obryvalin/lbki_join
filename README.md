# LBKI_JOIN — SQL-style JOIN для CSV файлов

Мощный инструмент для объединения CSV файлов с использованием SQL-подобных операций JOIN. Поддерживает различные разделители и типы объединений.

## 🎯 Возможности

- **Четыре типа JOIN**: INNER, LEFT, RIGHT, OUTER
- **Гибкие разделители**: Запятая (,), Точка с запятой (;), Табуляция (\t)
- **Независимые разделители**: Разные разделители для левого, правого и результирующего файлов
- **Автоопределение разделителя**: Автоматическое определение разделителя, если не указан
- **Статистика**: Вывод количества строк во входных и результирующем файлах
- **Два интерфейса**: Графический (GUI) и командная строка (CLI)

## 📋 Требования

- Python 3.6+
- Встроенные модули: `tkinter` (для GUI), `csv`, `argparse`

## 🚀 Установка

1. Клонируйте репозиторий или скачайте файлы:
```bash
git clone <repository-url>
cd lbki_join
```

2. Убедитесь, что установлен Python 3.6+:
```bash
python --version
```

## 📖 Использование

### Графический интерфейс (GUI)

Запустите приложение:
```bash
python lbki_join_gui.py
```

**Интерфейс включает:**
- Выбор левого и правого CSV файлов
- Выбор разделителя для каждого файла
- Выбор ключевых колонок для JOIN
- Выбор типа JOIN (INNER, LEFT, RIGHT, OUTER)
- Выбор разделителя для результирующего файла
- Выбор пути сохранения результата
- Отображение статистики

**Пример использования GUI:**
1. Нажмите "Обзор..." рядом с "Левый CSV" и выберите файл
2. Выберите разделитель для левого файла
3. Нажмите "Обзор..." рядом с "Правый CSV" и выберите файл
4. Выберите разделитель для правого файла
5. Выберите ключевые колонки из выпадающих списков
6. Выберите тип JOIN
7. Выберите разделитель для результирующего файла
8. Нажмите "Выбрать..." для выбора пути сохранения
9. Нажмите "Выполнить JOIN"
10. Просмотрите статистику в нижней части окна

### Командная строка (CLI)

Базовый синтаксис:
```bash
python lbki_join_cli.py <левый_файл> <правый_файл> -k1 <ключ_левый> -k2 <ключ_правый> -o <выходной_файл>
```

**Параметры:**
- `<левый_файл>` - путь к левому CSV файлу
- `<правый_файл>` - путь к правому CSV файлу
- `-k1, --left-key` - колонка в левом файле для JOIN (обязательно)
- `-k2, --right-key` - колонка в правом файле для JOIN (обязательно)
- `-t, --join-type` - тип JOIN: `inner`, `left`, `right`, `outer` (по умолчанию: `inner`)
- `-d1, --left-delimiter` - разделитель левого файла (если не указан — автоопределение)
- `-d2, --right-delimiter` - разделитель правого файла (если не указан — автоопределение)
- `-o, --output` - путь к выходному CSV файлу (обязательно)

**Примеры:**

INNER JOIN с автоопределением разделителей:
```bash
python lbki_join_cli.py left.csv right.csv -k1 id -k2 user_id -o result.csv
```

LEFT JOIN с явным указанием разделителей:
```bash
python lbki_join_cli.py left.csv right.csv -k1 id -k2 user_id -t left -d1 , -d2 ; -o result.csv
```

OUTER JOIN с табуляцией:
```bash
python lbki_join_cli.py left.csv right.csv -k1 id -k2 user_id -t outer -d1 '\t' -d2 '\t' -o result.csv
```

## 📊 Типы JOIN

### INNER JOIN
Возвращает только строки, где ключи совпадают в обоих файлах.

```
LEFT:          RIGHT:         RESULT (INNER):
id | name      id | age       id | name | age
1  | Alice     1  | 25        1  | Alice | 25
2  | Bob       3  | 30        3  | Charlie | 30
3  | Charlie
```

### LEFT JOIN
Возвращает все строки из левого файла и совпадающие строки из правого. Несовпадающие значения из правого файла заполняются пустыми строками.

```
LEFT:          RIGHT:         RESULT (LEFT):
id | name      id | age       id | name | age
1  | Alice     1  | 25        1  | Alice | 25
2  | Bob       3  | 30        2  | Bob | 
3  | Charlie                  3  | Charlie | 30
```

### RIGHT JOIN
Возвращает все строки из правого файла и совпадающие строки из левого. Несовпадающие значения из левого файла заполняются пустыми строками.

```
LEFT:          RIGHT:         RESULT (RIGHT):
id | name      id | age       id | name | age
1  | Alice     1  | 25        1  | Alice | 25
2  | Bob       3  | 30        3  | Charlie | 30
3  | Charlie   4  | 35        4  | | 35
```

### OUTER JOIN
Возвращает все строки из обоих файлов. Несовпадающие значения заполняются пустыми строками.

```
LEFT:          RIGHT:         RESULT (OUTER):
id | name      id | age       id | name | age
1  | Alice     1  | 25        1  | Alice | 25
2  | Bob       3  | 30        2  | Bob | 
3  | Charlie   4  | 35        3  | Charlie | 30
                               4  | | 35
```


## 🔧 Основные функции модуля lbki_join.py

### `detect_delimiter(file_path: str) -> str`
Автоматически определяет разделитель CSV файла.

### `read_csv(file_path: str, delimiter: Optional[str] = None) -> Tuple[List[str], List[Dict[str, str]]]`
Читает CSV файл и возвращает заголовки и данные.

### `join_data(left_data, right_data, left_key, right_key, join_type='inner') -> List[Dict[str, str]]`
Выполняет операцию JOIN над двумя наборами данных.

### `write_csv(data: List[Dict[str, str]], output_path: str, delimiter: str = ',')`
Записывает результат в CSV файл.

### `read_csv_with_delimiters(left_file, right_file, left_delimiter=None, right_delimiter=None)`
Читает оба CSV файла с разными разделителями.

## 📊 Пример работы

### Входные данные

**left.csv** (разделитель: запятая)
```
id,name,department
1,Alice,Sales
2,Bob,IT
3,Charlie,HR
```

**right.csv** (разделитель: точка с запятой)
```
id;salary;bonus
1;50000;5000
3;60000;6000
4;55000;5500
```

### Команда
```bash
python lbki_join_cli.py left.csv right.csv -k1 id -k2 id -t inner -d1 , -d2 ; -o result.csv
```

### Результат (result.csv)
```
id,name,department,salary,bonus
1,Alice,Sales,50000,5000
3,Charlie,HR,60000,6000
```

### Статистика
```
[✓] JOIN выполнен успешно!
──────────────────────────────────────────────────
Статистика:
  Левый файл (LEFT):     3 строк
  Правый файл (RIGHT):   3 строк
──────────────────────────────────────────────────
  Результат (INNER):      2 строк
──────────────────────────────────────────────────
Сохранено в: result.csv
```

## 🐛 Обработка ошибок

Приложение обрабатывает следующие ошибки:

- **Файл не найден** - проверьте путь к файлу
- **Неверный разделитель** - используйте `,`, `;` или `\t`
- **Ключ не найден** - проверьте названия колонок в файлах
- **Пустой файл** - убедитесь, что файл содержит данные
- **Кодировка** - файлы должны быть в кодировке UTF-8

## 💡 Советы и рекомендации

1. **Проверьте разделители**: Убедитесь, что вы правильно указали разделители для входных файлов
2. **Проверьте ключи**: Названия ключевых колонок должны точно совпадать (с учетом регистра)
3. **Резервная копия**: Сохраняйте исходные файлы перед обработкой
4. **Большие файлы**: Для очень больших файлов используйте CLI вместо GUI
5. **Автоопределение**: Если вы не уверены в разделителе, оставьте параметр пустым для автоопределения

## 📝 Примеры использования

### Пример 1: Простой INNER JOIN
```bash
python lbki_join_cli.py users.csv orders.csv -k1 user_id -k2 user_id -o result.csv
```

### Пример 2: LEFT JOIN с разными разделителями
```bash
python lbki_join_cli.py data1.csv data2.csv -k1 id -k2 id -t left -d1 , -d2 ; -o result.csv
```

### Пример 3: OUTER JOIN с табуляцией
```bash
python lbki_join_cli.py file1.tsv file2.tsv -k1 code -k2 code -t outer -d1 '\t' -d2 '\t' -o result.tsv
```

## 🤝 Вклад

Если вы нашли ошибку или хотите добавить новую функцию, пожалуйста, создайте issue или pull request.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 👨‍💻 Автор

Разработано как инструмент для работы с CSV файлами.

## 🔗 Ссылки

- [Python CSV документация](https://docs.python.org/3/library/csv.html)
- [SQL JOIN операции](https://en.wikipedia.org/wiki/Join_(SQL))

## ❓ Часто задаваемые вопросы

**Q: Как узнать, какой разделитель использует мой файл?**
A: Откройте файл в текстовом редакторе и посмотрите, какой символ разделяет колонки. Или используйте автоопределение, оставив параметр разделителя пустым.

**Q: Могу ли я использовать разные ключи для левого и правого файлов?**
A: Да! Это основная особенность инструмента. Используйте `-k1` для левого файла и `-k2` для правого файла.

**Q: Что произойдет, если ключ не найден в одном из файлов?**
A: Приложение выведет ошибку с указанием доступных колонок в файле.

**Q: Поддерживаются ли другие форматы, кроме CSV?**
A: Нет, инструмент работает только с CSV файлами. Однако вы можете конвертировать другие форматы в CSV.

**Q: Как обработать файлы с ��одировкой, отличной от UTF-8?**
A: Сначала конвертируйте файл в UTF-8 с помощью текстового редактора или инструмента командной строки.
